pre-commit:
  commands:
    format:
      glob: "*.{ts,tsx,js,jsx,json,jsonc,css,html,md,yml,yaml}"
      run: pnpm exec oxfmt --check {staged_files}
    lint:
      run: pnpm lint

pre-push:
  commands:
    lint:
      run: pnpm lint
    typecheck:
      run: pnpm typecheck
